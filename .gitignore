# Xcode
# XcodeGen(project.yml)から再生成できるため xcodeproj は追跡しない
*.xcodeproj
xcuserdata/
DerivedData/
*.xcresult
build/

# Swift Package Manager
.build/
.swiftpm/

# 実機自動再インストール(scripts/reinstall-device.sh)のビルド出力先
.build-device/
.build-sim/
.build-*/

# XcodeGen が生成する Info.plist / entitlements
TrackStack/Info.plist
TrackStackWidget/Info.plist
TrackStack/TrackStack.entitlements
TrackStackWidget/TrackStackWidget.entitlements

# macOS
.DS_Store
